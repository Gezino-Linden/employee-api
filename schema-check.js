require('dotenv').config(); const db = require('./src/db'); db.query('SELECT table_name, column_name, data_type FROM information_schema.columns WHERE table_schema =  ORDER BY table_name, ordinal_position', ['public']).then(r => { r.rows.forEach(row => console.log(row.table_name.padEnd(30), row.column_name.padEnd(30), row.data_type)); process.exit(); }).catch(e => { console.error(e.message); process.exit(1); });
